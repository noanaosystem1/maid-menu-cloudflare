# Cloudflare Worker 実行タイミング・トリガー一覧

本システムは、**Cloudflare Workers** と **Durable Objects (SQLite 搭載 + 冬眠API採用)** を組み合わせた構成になっており、静的アセット配信からリアルタイム通信用 WebSocket サーバーまでを単一の Worker エントリーポイント (`server/worker.js`) で受け止めています。

以下に、Worker および Durable Object が実行・起動されるタイミング（エンドポイント、HTTPメソッド、WebSocket接続イベント、および実行コンテキスト）を整理して一覧化します。

---

## 1. 静的アセット配信（フロントエンド画面アクセス）

ユーザーがブラウザでアプリケーションにアクセスした際、静的アセット（HTML, CSS, JS）を返すために Worker が起動します。

| トリガーパス | メソッド | 主な呼び出し元 | 概要 |
|:---|:---:|:---|:---|
| `/` | `GET` | ブラウザ (管理者画面) | 管理画面 (`/admin` にフォールバック) の静的アセットを返します。 |
| `/admin` | `GET` | ブラウザ (管理者画面) | 管理者用ダッシュボードの静的アセットを返します。 |
| `/guest` | `GET` | ブラウザ (ゲスト画面) | `?token=xxx` クエリを伴ってアクセスするゲスト用画面の静的アセットを返します。 |
| `/assets/*` | `GET` | ブラウザ (共通) | Viteビルドによって生成された CSS/JS および各種画像アセットを返します。 |
| *その他未定義パス* | `GET` | ブラウザ | 存在しないパスへのアクセス時、React-RouterによるフロントエンドSPA制御に繋げるため、フォールバックとして `index.html` の中身を返します。 |

---

## 2. リアルタイム通信および管理コマンド (WebSocket 起動トリガー)

管理画面やゲスト画面が双方向リアルタイム通信を開始、または維持するために実行されます。
本システムは**完全WebSocket化**されており、すべてのフェーズで WebSocket が常時接続（繋ぎっぱなし）されるとともに、管理コマンドもすべて WebSocket メッセージを介して処理されます。

| トリガーパス | メソッド | 主な呼び出し元 | 概要 |
|:---|:---:|:---|:---|
| `/api/ws` | `GET` | フロントエンド (React アプリ) | **WebSocket アップグレード要求**。WebSocket 接続を処理するために Worker が起動し、対応する Durable Object（`MaidCafeDO`）へ接続を中継（プロキシ）します。 |

### Cloudflare WebSocket Hibernation（冬眠）API による起動タイミング

Durable Object 内では、無駄なサーバー稼働コスト（GB-秒）を削減するため、メモリ上の変数空間への接続保持を廃止し、**冬眠 API** に接続管理を委ねています。これにより、以下のタイミングのみで Durable Object のコードが活性化・起動されます。

1. **接続アップグレード時 (`state.acceptWebSocket(ws)`)**:
   - クライアントが `/api/ws` に接続し、ハンドシェイクが成功した瞬間。
   - `ws.serializeAttachment()` を用いて、ソケット自身に `roomId` や `guestId`、初期ロール情報をアタッチします。
2. **WebSocket メッセージ受信時 (`webSocketMessage(ws, message)`)**:
   - クライアントからテキストメッセージ（JSON文字列）が送信されてきたタイミング。
   - **冬眠中だった場合、このメッセージ受信によって自動的に Durable Object インスタンスが活性化（復帰）して実行されます。**
   - **管理者用コマンドの実行:** 管理画面からフェーズ変更、ルーム制御、またはメニュー編集のコマンドが送信された際、メッセージ内のパスワード検証が行われ、成功すれば SQLite 内のデータ更新や他クライアントへのブロードキャストを実行します。
3. **クライアント切断・エラー時 (`webSocketClose(ws, code, reason)` / `webSocketError(ws, error)`)**:
   - ユーザーがタブを閉じる、またはネットワーク切断により WebSocket 接続が切断された瞬間。
   - アタッチされたメタデータ（`deserializeAttachment()`）を参照し、データベースの `is_online` 状態を `0` に安全に更新します。
4. **非アクティブ（冬眠）移行時**:
   - 一定時間メッセージのやり取りがない場合、Durable Object は自動的に「冬眠（Hibernation）」状態に入ります（開発者側の明示的なトリガーは不要で、Cloudflare の基盤が自動制御します）。

---

## 3. REST API 要求 (フロントエンドから呼び出される REST エンドポイント)

完全WebSocket化により、管理者の定期ポーリングや進行フェーズ変更は WebSocket に移行したため、これらの HTTP トリガーは発生しなくなりましたが、一部の非同期的なデータの初期化マスタデータの読み出しや、管理コマンド以外の静的操作にのみ REST API が動作します。

### 3.1 部屋（Rooms）制御

| トリガーパス | メソッド | 実行タイミング | 認証 | 主な処理 |
|:---|:---:|:---|:---:|:---|
| `/api/rooms` | `GET` | 画面初期ロード時 | 不要 | 登録されているすべての部屋情報を取得します。 |
| `/api/rooms/:id` | `GET` | ゲスト画面初期ロード時 | 不要 | 指定されたIDのルーム詳細（現在のフェーズなど）を初期ロードとして取得します。 |

### 3.2 ゲスト（Guests）制御

| トリガーパス | メソッド | 実行タイミング | 認証 | 主な処理 |
|:---|:---:|:---|:---:|:---|
| `/api/guests` | `GET` | ゲスト入室時のトークン検証時 | 不要 | `sessionToken` または `roomId` に紐づくゲスト自身の検証用に取得します。 |

### 3.3 メニュー（Menu Items）制御

| トリガーパス | メソッド | 実行タイミング | 認証 | 主な処理 |
|:---|:---:|:---|:---:|:---|
| `/api/menu-items` | `GET` | メニュー表示時、管理画面表示時 | 不要 | 登録されている全メニュー項目を、並び順（`order_index`）の昇順で取得します。 |

---

## 4. ヘルスチェック（システム生存確認）

外部の死活監視システムやデプロイ検証用スクリプトから定期的に呼び出されます。

| トリガーパス | メソッド | 実行タイミング | 認証 | 概要 |
|:---|:---:|:---|:---:|:---|
| `/api/health` | `GET` | 外部サービス監視、テスト実行時 | 不要 | Durable Object 内の SQLite が正常に動作しているか生存確認応答を返します。 |

---

## 5. プリフライト（CORS）対応

他ドメインからブラウザが安全にリクエストを送れるように、ブラウザ側が本番リクエストを投げる直前に自動的に実行されます。

| トリガーパス | メソッド | 実行タイミング | 概要 |
|:---|:---:|:---|:---|
| `*` (全パス共通) | `OPTIONS` | ブラウザのセキュリティチェック時 | CORS 許可ヘッダー（`Access-Control-Allow-Origin`, `Access-Control-Allow-Methods` 等）を返し、安全な通信であることをブラウザに通知します（Durable Object へのアクセスは発生せず、Worker のみで即座に応答を返します）。 |
