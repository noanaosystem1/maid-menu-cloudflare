# Cloudflare Worker 実行タイミング・トリガー一覧

本システムは、**Cloudflare Workers** と **Durable Objects (SQLite 搭載)** を組み合わせた構成になっており、静的アセット配信からリアルタイム通信用 WebSocket、REST API までを単一の Worker エントリーポイント (`server/worker.js`) で受け止めています。

以下に、Worker が実行されるすべてのタイミング（エンドポイント、HTTPメソッド、WebSocket接続イベント、および実行コンテキスト）を整理して一覧化します。

---

## 1. 静的アセット配信（フロントエンド画面アクセス）

ユーザーがブラウザでアプリケーションにアクセスした際、静的アセット（HTML, CSS, JS）を返すために Worker が起動します。

| トリガーパス | メソッド | 主な呼び出し元 | 概要 |
|:---|:---:|:---|:---|
| `/` | `GET` | ブラウザ (管理者画面) | 管理画面 (`/admin` にフォールバック) の静的アセットを返します。 |
| `/admin` | `GET` | ブラウザ (管理者画面) | 管理者用ダッシュボードの静的アセットを返します。 |
| `/guest` | `GET` | ブラウザ (ゲスト画面) | `?token=xxx` クエリを伴ってアクセスするゲスト用画面の静的アセットを返します。 |
| `/assets/*` | `GET` | ブラウザ (共通) | Viteビルドによって生成された CSS/JS および各種画像アセットを返します。 |
| *その他未定義パス* | `GET` | ブラウザ | 存在しないパスへのアクセス時、React-RouterによるフロントエンドSPA制御に繋げるため、フォールバックとして `index.html` の中身を返します。 |

---

## 2. リアルタイム通信 (WebSocket トリガー)

管理画面やゲスト画面が双方向リアルタイム通信を開始、または維持するために実行されます。

| トリガーパス | メソッド | 主な呼び出し元 | 概要 |
|:---|:---:|:---|:---|
| `/api/ws` | `GET` | フロントエンド (React アプリ) | **WebSocket アップグレード要求**。WebSocket接続を処理するために Worker が起動し、接続先ルーム (`roomId`) とゲスト情報 (`guestId`) を判定して、対応する Durable Object インスタンスへ処理を中継 (プロキシ) します。 |

### WebSocket 接続確立後の Durable Object 内実行タイミング
WebSocket が接続された後、以下の非同期イベント・タイミングで Durable Object（`MaidCafeDO`）側のコードが稼働します。

1. **接続開始 (`ws.accept()`)**:
   - 新しいクライアントが接続した瞬間。
   - データベース（SQLite）の `guest_users` テーブルの対象ユーザーの `is_online` を `1` に更新し、`last_seen` を現在日時に更新します。
   - 接続直後、対象クライアントへ現在のルーム演出フェーズ (`PHASE_UPDATE`) をメッセージ送信します。
2. **メッセージ受信 (`ws.onmessage`)**:
   - クライアントから何らかのデータが送られてきたタイミング。
3. **切断時 (`ws.onclose` / `ws.onerror`)**:
   - ネットワーク遮断、タブ閉じ、または意図的な切断時。
   - `guest_users` テーブルの対象ユーザーの `is_online` を `0` に更新し、`last_seen` を現在日時に更新したうえで、該当ルームの全接続クライアントに `GUEST_UPDATE` メッセージをブロードキャストします。

---

## 3. REST API 要求 (フロントエンドから呼び出される REST エンドポイント)

フロントエンド React アプリからデータベース情報の取得・変更（CRUD操作）を行うため、HTTP リクエストをトリガーに Worker が起動します。
すべての API 操作は、Worker から Durable Object（`MaidCafeDO`）内の SQLite へ転送されて実行されます。

*※ REST API への書き込み操作 (`POST`, `PATCH`, `DELETE`)、およびクエリパラメータなしのゲスト一覧取得には管理者認証ヘッダー（`X-Admin-Password` または `Authorization`）が必要です。*

### 3.1 部屋（Rooms）制御

| トリガーパス | メソッド | 実行タイミング | 認証 | 主な処理 |
|:---|:---:|:---|:---:|:---|
| `/api/rooms` | `GET` | 管理画面表示時（周期ポーリング） | 不要 | 登録されているすべての部屋情報を取得します。 |
| `/api/rooms` | `POST` | 新規ルーム作成時 | **要** | 新しいルームを生成（UUID自動付与）し、SQLite に追加します。 |
| `/api/rooms/:id` | `GET` | ゲスト画面初期ロード時 | 不要 | 指定されたIDのルーム詳細（現在のフェーズなど）を取得します。 |
| `/api/rooms/:id` | `PATCH` | 管理画面から進行フェーズを変更した時 | **要** | 部屋情報（名前やフェーズ）を更新します。フェーズ変更時は接続中クライアント全員に **`PHASE_UPDATE` を一斉ブロードキャスト** します。 |
| `/api/rooms/:id` | `DELETE` | 管理画面から部屋を削除した時 | **要** | 指定したルームを削除します。紐づいていた全ゲストも自動でカスケード削除されます。 |

### 3.2 ゲスト（Guests）制御

| トリガーパス | メソッド | 実行タイミング | 認証 | 主な処理 |
|:---|:---:|:---|:---:|:---|
| `/api/guests` | `GET` | ・管理画面のダッシュボード表示時<br>・ゲスト入室時のトークン検証時 | 条件付 | ・`sessionToken` または `roomId` パラメータ付きのクエリは**認証不要**（ゲスト自身の検証用）。<br>・パラメータなしの全件取得は**要認証**。 |
| `/api/guests` | `POST` | 管理者がゲスト用招待リンクを作成した時 | **要** | 新規ゲストを登録します。 |
| `/api/guests/:id` | `PATCH` | ゲストのステータスや名前を変更する時 | 不要 | ゲスト情報を更新します。更新後は接続中クライアントに `GUEST_UPDATE` をブロードキャストします。<br>*(※一般ポーリングからの自動更新を妨げないため、PATCH :id は特例で認証不要となっています)* |
| `/api/guests/:id` | `DELETE` | 管理画面からゲストを退室・削除する時 | **要** | データベースからゲストを物理削除します。 |

### 3.3 メニュー（Menu Items）制御

| トリガーパス | メソッド | 実行タイミング | 認証 | 主な処理 |
|:---|:---:|:---|:---:|:---|
| `/api/menu-items` | `GET` | ゲスト画面のメニュー表示時、管理画面表示時 | 不要 | 登録されている全メニュー項目を、並び順（`order_index`）の昇順で取得します。 |
| `/api/menu-items` | `POST` | メニュー管理画面から新メニューを追加した時 | **要** | 新しいメニューを SQLite に追加します。 |
| `/api/menu-items/:id` | `PATCH` | メニューの価格・名前・並び順を変更した時 | **要** | 指定したメニュー情報を更新します。 |
| `/api/menu-items/:id` | `DELETE` | メニュー管理画面から項目を削除した時 | **要** | 対象のメニューを削除します。 |

---

## 4. ヘルスチェック（システム生存確認）

外部の死活監視システムやデプロイ検証用スクリプトから定期的に呼び出されます。

| トリガーパス | メソッド | 実行タイミング | 認証 | 概要 |
|:---|:---:|:---|:---:|:---|
| `/api/health` | `GET` | 外部サービス監視、テスト実行時 | 不要 | Durable Object 内の SQLite が正常に動作しているか生存確認応答を返します。 |

---

## 5. プリフライト（CORS）対応

他ドメインからブラウザが REST API へ安全にリクエストを送れるように、ブラウザ側が本番リクエストを投げる直前に自動的に実行されます。

| トリガーパス | メソッド | 実行タイミング | 概要 |
|:---|:---:|:---|:---|
| `*` (全パス共通) | `OPTIONS` | ブラウザのセキュリティチェック時 | CORS 許可ヘッダー（`Access-Control-Allow-Origin`, `Access-Control-Allow-Methods` 等）を返し、安全な通信であることをブラウザに通知します（Durable Object へのアクセスは発生せず、Worker のみで即座に応答を返します）。 |
